#define MAX_SIZE 9



int narrow_possible(int s, int conditions[MAX_SIZE][2], int size, int arr[MAX_SIZE][MAX_SIZE])
{
    int x;
    int y;

    x = s / size;
    y = s % size;

    if (val == 1)
        arr[x][y] = 1 << size;
    else if (val == size)
    {

    }
}

int	eliminate(int s, int arr[MAX_SIZE][MAX_SIZE], int conditions[MAX_SIZE][2], int size)
{
    


}